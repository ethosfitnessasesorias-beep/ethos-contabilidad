import ContenidoPantalla from "../pantalla";

// Calendario de publicaciones (sincronizado con Google Calendar vía .ics)
export default function CalendarioContenidoPage() {
  return <ContenidoPantalla vista="calendario" />;
}
