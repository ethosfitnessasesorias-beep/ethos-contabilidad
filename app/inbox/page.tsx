import { EnConstruccion } from "../enconstruccion";

export default function Page() {
  return (
    <EnConstruccion
      titulo="Inbox"
      descripcion="Mensajes y comunicaciones con clientes centralizados en un solo sitio."
    />
  );
}
