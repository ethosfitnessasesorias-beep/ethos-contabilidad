import { EnConstruccion } from "../enconstruccion";

export default function Page() {
  return (
    <EnConstruccion
      titulo="Actividades"
      descripcion="Llamadas, visitas y tareas ligadas a cada cliente o deal, con recordatorios."
    />
  );
}
