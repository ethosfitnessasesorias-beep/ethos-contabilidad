import { EnConstruccion } from "../enconstruccion";

export default function Page() {
  return (
    <EnConstruccion
      titulo="Contactos"
      descripcion="Lista de contactos y leads con su canal, responsable y estado en el pipeline."
    />
  );
}
