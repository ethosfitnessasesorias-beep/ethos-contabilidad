import { EnConstruccion } from "../enconstruccion";

export default function Page() {
  return (
    <EnConstruccion
      titulo="Pipeline"
      descripcion="Tablero de deals arrastrables por fases (Lead → Contactado → Agendado → Ganado)."
    />
  );
}
