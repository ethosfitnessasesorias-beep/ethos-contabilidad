import { EnConstruccion } from "../enconstruccion";

export default function Page() {
  return (
    <EnConstruccion
      titulo="Reportes"
      descripcion="Informes de facturación, cash collected, reparto y evolución por canal."
    />
  );
}
